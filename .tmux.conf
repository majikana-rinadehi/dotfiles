set -g prefix C-s
unbind C-b

set-option -g default-terminal screen-256color
set -g terminal-overrides 'xterm:colors=256'

setw -g base-index 1
setw -g pane-base-index 1

bind r source-file ~/.tmux.conf \; display "Config Reloaded!"
bind | split-window -h
bind - split-window -v
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

set-option -g status-left '[#P]'
set-option -g status-right '[%Y-%m-%d(%a) %H:%M]'
set -g window-style 'fg=white,bg=colour234'
set -g window-active-style 'fg=white,bg=black'

## ウィンドウ 1 → 青
#tmux setw -t 1 window-style 'bg=blue'
#tmux setw -t 1 window-status-style 'fg=white,bg=blue'
#
## ウィンドウ 2 → 緑
#tmux setw -t 2 window-style 'bg=green'
#tmux setw -t 2 window-status-style 'fg=white,bg=green'
#
## ウィンドウ 3 → 赤
#tmux setw -t 3 window-style 'bg=red'
#tmux setw -t 3 window-status-style 'fg=white,bg=red'
